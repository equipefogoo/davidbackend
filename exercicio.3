1. Escreva um programa que mostre na tela a seguinte contagem: 6 7 8 9 10 11
Acabou!

for (let i = 6; i <= 11; i++) {
  process.stdout.write(i + " ");
}
console.log("\nAcabou!");


2. Faça um algoritmo que mostre na tela a seguinte contagem: 10 9 8 7 6 5 4 3
Acabou!

for (let i = 10; i >= 3; i--) {
  process.stdout.write(i + " ");
}
console.log("\nAcabou!");


3. Crie um aplicativo que mostre na tela a seguinte contagem: 0 3 6 9 12 15 18
Acabou!

for (let i = 0; i <= 18; i += 3) {
  process.stdout.write(i + " ");
}
console.log("\nAcabou!");


4. Desenvolva um programa que mostre na tela a seguinte contagem: 100 95 90 85 80
... 0 Acabou!

for (let i = 100; i >= 0; i -= 5) {
  process.stdout.write(i + " ");
}
console.log("\nAcabou!");


5. Faça um algoritmo que pergunte ao usuário um número inteiro e positivo qualquer e
mostre uma contagem até esse valor: Ex: Digite um valor: 35 Contagem: 1 2 3 4 5 6
7 ... 33 34 35 Acabou!

const prompt = require("prompt-sync")();

let valor = parseInt(prompt("Digite um valor: "));

for (let i = 1; i <= valor; i++) {
  process.stdout.write(i + " ");
}
console.log("\nAcabou!");


6. Desenvolva um algoritmo que mostre uma contagem regressiva de 30 até 1,
marcando os números que forem divisíveis por 4, exatamente como mostrado
abaixo: 30 29 [28] 27 26 25 [24] 23 22 21 [20] 19 18 17 [16]...

for (let i = 30; i >= 1; i--) {
  if (i % 4 === 0) {
    process.stdout.write("[" + i + "] ");
  } else {
    process.stdout.write(i + " ");
  }
}
console.log("\nAcabou!");


7. Crie um algoritmo que leia o valor inicial da contagem, o valor final e o incremento,
mostrando em seguida todos os valores no intervalo: Ex: Digite o primeiro Valor: 3
Digite o último Valor: 10 Digite o incremento: 2 Contagem: 3 5 7 9 Acabou!
const prompt = require("prompt-sync")();

let inicio = parseInt(prompt("Primeiro valor: "));
let fim = parseInt(prompt("Último valor: "));
let inc = parseInt(prompt("Incremento: "));

for (let i = inicio; i <= fim; i += inc) {
  process.stdout.write(i + " ");
}
console.log("\nAcabou!");


8. O programa acima vai ter um problema quando digitarmos o primeiro valor maior
que o último. Resolva esse problema com um código que funcione em qualquer
situação.
const prompt = require("prompt-sync")();

let inicio = parseInt(prompt("Primeiro valor: "));
let fim = parseInt(prompt("Último valor: "));
let inc = parseInt(prompt("Incremento: "));

if (inicio < fim) {
  for (let i = inicio; i <= fim; i += inc) {
    process.stdout.write(i + " ");
  }
} else {
  for (let i = inicio; i >= fim; i -= inc) {
    process.stdout.write(i + " ");
  }
}

console.log("\nAcabou!");


9. Crie um programa que calcule e mostre na tela o resultado da soma entre 6 + 8 + 10
+ 12 + 14 + ... + 98 + 100.
let soma = 0;

for (let i = 6; i <= 100; i += 2) {
  soma += i;
}

console.log("Soma =", soma);


10. Desenvolva um aplicativo que mostre na tela o resultado da expressão 500 + 450 +
400 + 350 + 300 + ... + 50 + 0.
let soma = 0;

for (let i = 500; i >= 0; i -= 50) {
  soma += i;
}

console.log("Resultado =", soma);


11. Faça um programa que leia 7 números inteiros e no final mostre o somatório entre
eles.
const prompt = require("prompt-sync")();

let soma = 0;

for (let i = 1; i <= 7; i++) {
  let num = parseInt(prompt(`Digite o ${i}º número: `));
  soma += num;
}

console.log("Somatório =", soma);


12. Crie um programa que leia 6 números inteiros e no final mostre quantos deles são
pares e quantos são ímpares.

const prompt = require("prompt-sync")();

let pares = 0;
let impares = 0;

for (let i = 1; i <= 6; i++) {
  let num = parseInt(prompt(`Digite o ${i}º número: `));

  if (num % 2 === 0) pares++;
  else impares++;
}

console.log("Pares:", pares);
console.log("Ímpares:", impares);


13. Desenvolva um programa que faça o sorteio de 20 números entre 0 e 10 e mostre
na tela:
a) Quais foram os números sorteados
b) Quantos números estão acima de 5
c) Quantos números são divisíveis por 3

let sorteados = [];
let acima5 = 0;
let div3 = 0;

for (let i = 1; i <= 20; i++) {
  let num = Math.floor(Math.random() * 11);
  sorteados.push(num);

  if (num > 5) acima5++;
  if (num % 3 === 0) div3++;
}

console.log("Números sorteados:", sorteados.join(" "));
console.log("Acima de 5:", acima5);
console.log("Divisíveis por 3:", div3);


14. Faça um aplicativo que leia o preço de 8 produtos. No final, mostre na tela qual foi o
maior e qual foi o menor preço digitados.

const prompt = require("prompt-sync")();

let maior = 0;
let menor = Infinity;

for (let i = 1; i <= 8; i++) {
  let preco = parseFloat(prompt(`Preço do produto ${i}: `));

  if (preco > maior) maior = preco;
  if (preco < menor) menor = preco;
}

console.log("Maior preço:", maior);
console.log("Menor preço:", menor);


15. Crie um algoritmo que leia a idade de 10 pessoas, mostrando no final:
a) Qual é a média de idade do grupo 
b) Quantas pessoas tem mais de 18 anos
c) Quantas pessoas tem menos de 5 anos 
d) Qual foi a maior idade lida
const prompt = require("prompt-sync")();

let soma = 0;
let maior = 0;
let acima18 = 0;
let abaixo5 = 0;

for (let i = 1; i <= 10; i++) {
  let idade = parseInt(prompt(`Idade da pessoa ${i}: `));

  soma += idade;
  if (idade > maior) maior = idade;
  if (idade > 18) acima18++;
  if (idade < 5) abaixo5++;
}

console.log("Média:", soma / 10);
console.log("Maiores de 18:", acima18);
console.log("Menores de 5:", abaixo5);
console.log("Maior idade:", maior);


16. Faça um programa que leia a idade e o sexo de 5 pessoas, mostrando no final:
a)Quantos homens foram cadastrados
 b) Quantas mulheres foram cadastradas
 c) A média de idade do grupo
 d) A média de idade dos homens 
 e) Quantas mulheres tem mais de 20 anos
 const prompt = require("prompt-sync")();

let homens = 0, mulheres = 0;
let soma = 0, somaHomens = 0;
let mulheres20 = 0;

for (let i = 1; i <= 5; i++) {
  let idade = parseInt(prompt("Idade: "));
  let sexo = prompt("Sexo (M/F): ").toUpperCase();

  soma += idade;

  if (sexo === "M") {
    homens++;
    somaHomens += idade;
  } else {
    mulheres++;
    if (idade > 20) mulheres20++;
  }
}

console.log("Homens:", homens);
console.log("Mulheres:", mulheres);
console.log("Média grupo:", soma / 5);
console.log("Média homens:", homens > 0 ? somaHomens / homens : 0);
console.log("Mulheres > 20:", mulheres20);


17. Desenvolva um aplicativo que leia o peso e a altura de 7 pessoas, mostrando no
final: 
a) Qual foi a média de altura do grupo 
b) Quantas pessoas pesam mais de 90Kg 
c) Quantas pessoas que pesam menos de 50Kg tem menos de 1.60m 
d)Quantas pessoas que medem mais de 1.90m pesam mais de 100Kg.

const prompt = require("prompt-sync")();

let somaAltura = 0;
let acima90 = 0;
let abaixo50e160 = 0;
let acima190e100 = 0;

for (let i = 1; i <= 7; i++) {
  let peso = parseFloat(prompt("Peso: "));
  let altura = parseFloat(prompt("Altura: "));

  somaAltura += altura;

  if (peso > 90) acima90++;
  if (peso < 50 && altura < 1.60) abaixo50e160++;
  if (altura > 1.90 && peso > 100) acima190e100++;
}

console.log("Média altura:", somaAltura / 7);
console.log("Acima de 90kg:", acima90);
console.log("Menos de 50kg e <1.60:", abaixo50e160);
console.log("Mais de 1.90m e >100kg:", acima190e100);


18. $$DESAFIO$$
Vamos melhorar o jogo. A partir de agora, o
computador vai sortear um número entre 1 e 10 e o jogador vai ter 4 tentativas para
tentar acertar.

const prompt = require("prompt-sync")();

let sorteado = Math.floor(Math.random() * 10) + 1;
let tentativas = 4;
let acertou = false;

while (tentativas > 0) {
  let palpite = parseInt(prompt("Digite um número entre 1 e 10: "));

  if (palpite === sorteado) {
    console.log("🎉 Parabéns! Você acertou!");
    acertou = true;
    break;
  } else {
    tentativas--;
    console.log("❌ Errou! Tentativas restantes:", tentativas);
  }
}

if (!acertou) {
  console.log("😢 Você perdeu! O número era:", sorteado);
}
